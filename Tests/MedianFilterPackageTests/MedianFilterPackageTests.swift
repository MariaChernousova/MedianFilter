import XCTest
@testable import MedianFilterPackage

final class MedianFilterPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(MedianFilterPackage().text, "Hello, World!")
    }
}
